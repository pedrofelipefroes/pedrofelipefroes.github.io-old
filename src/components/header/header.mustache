{{#header}}
	<header class="c-header">
		<h2 class="c-header__title">{{title}}</h2>
		<section class="c-header__info">
			<p class="u-m-bottom--2">{{subtitle}}</p>
			<h4 class="c-header__info__name">
				{{#description}}
					<span>{{.}}</span>
				{{/description}}
			</h4>
			<ul class="c-header__contact">
				{{#contact}}
				<li>
					<a href="{{url}}">{{name}}</a>
				</li>
				{{/contact}}
			</ul>
		</section>
	</header>
{{/header}}