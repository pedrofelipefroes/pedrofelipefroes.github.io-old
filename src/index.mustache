<!DOCTYPE html>
<html lang="en-US" prefix="og: http://ogp.me/ns#">

<head>
	<meta charset="UTF-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1" />

	<meta property="og:title" content="{{index.og.title}}" />
	<meta property="og:type" content="website" />
	<meta property="og:description" content="{{index.og.description}}" />
	<meta property="og:url" content="{{index.url}}" />
	<meta property="og:image" content="{{index.og.img.main}}" />
	<meta property="og:image:secure_url" content="{{index.og.img.secure_url}}" />
	<meta property="og:image:type" content="{{index.og.img.type}}" />
	<meta property="og:image:width" content="{{index.og.img.width}}" />
	<meta property="og:image:height" content="{{index.og.img.height}}" />

	<link rel="stylesheet" href="index.css" />
</head>

<body class="c-body">
	<header class="c-header">
		<h1>I am Froes, Designer and Developer.</h1>
		<p class="c-header__description">I believe design and code should intertwine to create whatever we imagine. <br />From elaborated graphics to thought-through design systems to practical CSS architectures,<br /> I'm an experienced liaison between the design and development.</p>
		<ul class="c-header__nav">
			{{#nav}}
				<li>
					<strong><a href="{{url}}">{{label}}</a></strong>
				</li>
			{{/nav}}
		</ul>
	</header>
	<main class="c-card-list">
		{{#card-list}}
			<a href="{{url}}" class="c-card">
				<figure class="c-card__thumbnail" style="background-image: url({{thumbnail}});"></figure>
				<div class="c-card__title">
					<small class="o-tag"><strong>{{tags}}</strong></small>
					<p><strong>{{name}}</strong></p>
				</div>
			</a>
		{{/card-list}}
	</main>

	{{> ../src/partials/footer/footer }}
</body>

</html>